import RestaurantMenuPage, { generateMetadata as generateMenuMetadata } from "../page";

export const dynamic = "force-dynamic";

export { generateMenuMetadata as generateMetadata };

export default RestaurantMenuPage;
